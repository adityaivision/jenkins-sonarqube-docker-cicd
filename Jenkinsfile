pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                //git branch: 'main', url: 'https://github.com/adityaivision/jenkins-sonarqube-docker-cicd.git'
            }
        }

        stage('Build') {
            steps {
                echo "Webhook triggered build successfully"
            }
        }
    }
}
